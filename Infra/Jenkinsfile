pipeline {
    agent any
    stages {
        stage('Print Variables') {
            steps {
                script {
                    echo "payload: ${env.payload}"
                }
            }
        }
    }
}
