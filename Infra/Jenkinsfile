pipeline {
    agent any
    stages {
        stage('Print Variables') {
            steps {
                script {
                    echo "Action: ${env.OBJECT_ATTRIBUTES_ACTION}"
                }
            }
        }
    }
}