pipeline {
    agent any
    stages {
        stage('Print Variables') {
            steps {
                script {
                    echo "State: ${env.object_attributes.action}"
                }
            }
        }
    }
}
