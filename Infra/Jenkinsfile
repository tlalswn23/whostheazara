pipeline {
    agent any
    stages {
        stage('Print Variables') {
            steps {
                script {
                    echo "object_attributes: ${env.object_attributes}"
                }
            }
        }
    }
}
