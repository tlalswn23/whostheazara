pipeline {
    agent any
    stages {
        stage('Print Variables') {
            steps {
                script {
                    echo "ref: ${env.ref}"
                }
            }
        }
    }
}
